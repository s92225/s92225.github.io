# Candy Jelly 3D 物件模型庫 v2

`src/object-catalog.js` 是獎品資料的唯一來源。每個記錄同時保存玩家名稱、收藏資料、3D model ID、半尺寸、重量及抓握係數；`src/object-models.js` 按 model ID 組合真正的 Three.js 3D 外觀。關卡只引用 `kind`，因此更改名稱時不會意外保留通用盒模型。

## 首批日常物件

| kind | model ID | 可見組件 | 完整碰撞尺寸 | 重量 | 抓握 |
|---|---|---|---:|---:|---:|
| `tissue` | `tissue-pack-v1` | 軟包裝、抽取口、白色紙巾、正面標籤 | 0.92 × 0.64 × 0.60 | 0.12 | 1.50 |
| `phone` | `smartphone-v1` | 圓角機殼、黑色螢幕、三鏡頭、手勢橫條 | 0.74 × 0.40 × 0.44 | 0.12 | 1.35 |
| `mouse` | `computer-mouse-v1` | 拱形外殼、左右鍵、滾輪、中線 | 0.62 × 0.56 × 0.54 | 0.12 | 1.35 |
| `bottle` | `water-bottle-v1` | 半透明樽身、樽肩、樽頸、樽蓋、標籤 | 0.50 × 0.88 × 0.50 | 0.16 | 1.35 |

## 第二批日常物件

第 9–30 關加入 22 個獨立模型：咖啡杯、遙控器、鎖匙、眼鏡、筆記簿、原子筆、銀包、梳、耳機盒、縮骨遮、番梘、牙膏、相機、手錶、波鞋、太陽帽、飯盒、充電器、電筒、玩具車、湯匙及膠紙座。每款都有唯一 model ID、至少四個識別組件、獨立圖鑑號碼及圖示。

尺寸是遊戲世界比例，先以手機畫面的辨認度和現有三爪間距為準，並非真實厘米。26 款日常物件均要通過：

1. `validatePrize()` 的爪距、長度、高度、重量及抓握範圍檢查。
2. 首批四款以多組瞄準偏差測試；新增 22 款逐件完成置中夾取及出貨測試。
3. 名稱、model ID 及可見組件完整性測試，防止退回通用盒外觀。

## 加入新物件

1. 在 `OBJECT_CATALOG` 新增名稱、model ID、收藏資料及物理規格。
2. 在 `object-models.js` 為該 model ID 建立可辨認的多組件模型。
3. 把物件加入關卡資料，再跑尺寸驗證及實際難度物理測試。

目前使用本機程式碼資料庫已足夠；日後需要大量關卡編輯、多人協作或遠端更新時，可原樣把 catalog 欄位搬到 JSON／後台資料庫，而不需重寫物理及渲染介面。

